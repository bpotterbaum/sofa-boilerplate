!!!
%html
%head
  %meta{charset:'utf-8'}/
  %meta{content:'IE=edge,chrome=1', 'http-equiv' => 'X-UA-Compatible'}/
  %meta{content:'width=670 user-scalable=yes', name:'viewport'}/
  %title= content_for?(:title) ? yield(:title) : 'Vineya'
  %meta{content:content_for?(:description) ? yield(:description) : '', name:'description'}/
  %meta{content:content_for?(:author) ? yield(:author) : 'CSD Dev team', name:'author'}/
  = stylesheet_link_tag 'marcom', :media => 'screen'

  = yield(:head)
  %body
    = yield
